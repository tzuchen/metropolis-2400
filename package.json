{
  "name": "metropolis-2400",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 2400",
    "build": "tsc && vite build",
    "preview": "vite preview --port 2400",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && npx tsx scripts/run-all-tests.ts",
    "benchmark": "npx tsx scripts/benchmark-performance.ts"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.9.0",
    "vite": "^6.0.0"
  }
}

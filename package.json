{
  "name": "metropolis-2400",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && npx tsx scripts/verify-game.ts && npx tsx scripts/verify-ai.ts && npx tsx scripts/verify-depth.ts && npx tsx scripts/verify-story.ts && npx tsx scripts/verify-augments.ts && npx tsx scripts/verify-weapons.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^6.0.0"
  }
}

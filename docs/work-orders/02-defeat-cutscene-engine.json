{
  "root": "/home/orin/metropolis-2400",
  "objective": "Modularize src/game.ts by extracting player defeat cinematic and detention transition logic into src/defeatCutscene.ts with 100% backward compatibility.",
  "allowed_files": [
    "src/defeatCutscene.ts",
    "src/game.ts"
  ],
  "tasks": [
    {
      "file": "src/defeatCutscene.ts",
      "objective": "Create src/defeatCutscene.ts exporting DefeatCutsceneHost interface and DefeatCutsceneManager class to manage the 3-stage defeat cutscene (swarm, blur_out, wake_up) and detention cell relocation logic from src/game.ts.",
      "constraints": [
        "Import bgm with static ESM import: `import { bgm } from './music';` (NEVER use require).",
        "Accept DefeatCutsceneHost in constructor: host has player, robots, map, securityLevel, checkInAlertActive, language, groundItems, sectorGroundItems, confiscatedGear, isGearConfiscated, defeatCutscene, laserBeams, switchSector, resetDetentionCell, updateFOV, render, pushFloatingText, pushMessage.",
        "Implement handlePlayerDefeat(instant?: boolean), executeDetentionRelocation(showMessages?: boolean), startDefeatCutscene(), updateDefeatCutscene(now: number) exactly matching current game.ts behavior.",
        "Preserve exact cutscene timing (swarm: 2200ms, blur_out: 1600ms, wake_up: 1400ms), detention cell coordinates (35, 5), evidence locker at (32, 6), and BGM exploration intensity."
      ],
      "context_files": [
        "src/game.ts",
        "src/types.ts",
        "scripts/verify-defeat-cutscene.ts"
      ],
      "verification_command": "npm run typecheck",
      "max_retries": 2
    },
    {
      "file": "src/game.ts",
      "objective": "Refactor src/game.ts to instantiate DefeatCutsceneManager as `private defeatCutsceneManager: DefeatCutsceneManager;` in GameEngine and delegate handlePlayerDefeat, executeDetentionRelocation, startDefeatCutscene, updateDefeatCutscene to it.",
      "constraints": [
        "Add import: `import { DefeatCutsceneManager } from './defeatCutscene';`",
        "In GameEngine constructor initialize: `this.defeatCutsceneManager = new DefeatCutsceneManager(this);`",
        "Delegate handlePlayerDefeat, executeDetentionRelocation, startDefeatCutscene, updateDefeatCutscene to this.defeatCutsceneManager while preserving public method signatures intact.",
        "Remove the duplicated original implementations of those 4 methods to slim down src/game.ts."
      ],
      "context_files": [
        "src/defeatCutscene.ts",
        "scripts/verify-defeat-cutscene.ts"
      ],
      "verification_command": "npx tsx scripts/verify-defeat-cutscene.ts",
      "max_retries": 2
    }
  ],
  "final_verification_commands": [
    "npm run typecheck",
    "npx tsx scripts/verify-defeat-cutscene.ts",
    "npm test"
  ]
}
